## Step 2: Delete Files and Directories

The `rm` command deletes files, and `rm -r` deletes directories.

### Your task
1. Delete the file `~/lab-workspace/docs/drafts/tmp_file.log`.
2. Delete the entire directory `~/lab-workspace/to_delete_dir/` and its contents.

<details>
<summary>Hint</summary>

```bash
rm ~/lab-workspace/docs/drafts/tmp_file.log
rm -r ~/lab-workspace/to_delete_dir/
```
</details>
