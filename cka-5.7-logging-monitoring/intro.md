## Logging & Monitoring — Container Logs, Events & Resource Usage

Welcome to this hands-on lab from [KubeDojo](https://kube-dojo.github.io/) — free, open-source cloud native education.

You have a real Kubernetes cluster with one control plane node ready to use.

### Objectives

In this lab you will:

- Use kubectl logs to inspect container output including multi-container pods
- Query and filter cluster events for troubleshooting
- Monitor resource usage using kubectl top and node-level tools

### Before You Start

- Read the [module page](https://kube-dojo.github.io/k8s/cka/part5-troubleshooting/module-5.7-logging-monitoring/) for theory
- This lab takes approximately **35 minutes**
- `kubectl` is already configured and ready
- Alias `k=kubectl` is set up for speed

Let's begin!
