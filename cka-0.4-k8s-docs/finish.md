# Congratulations!

You have completed the Kubernetes Documentation lab. You now know how to:

- Use `kubectl explain` to explore API resource fields
- Use `kubectl api-resources` to find resource types and short names
- Generate YAML with `--dry-run=client -o yaml` instead of writing from scratch

These techniques will save you significant time during the CKA exam. Always generate YAML from the CLI first, then modify it as needed.

## Next Lab

Continue to [CKA 0.5: Exam Strategy](https://killercoda.com/kubedojo/scenario/cka-0.5-exam-strategy) to learn time management and prioritization for the CKA exam.
