#!/bin/bash
# Verify step completion
# Exit 0 = pass, Exit 1 = fail
# Use exact checks, not string matching on output

# Example: check if a file exists
# [ -f /home/user/myfile.txt ] && exit 0 || exit 1

echo "TODO: Add verification"
exit 1
