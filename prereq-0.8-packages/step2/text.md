## Step 2: Install and Verify Packages
1. Install the `wget` package.
2. Verify it is installed by running `wget --version` and saving the output to `~/wget-version.txt`.
<details>
<summary>Hint</summary>
```bash
sudo apt install -y wget
wget --version > ~/wget-version.txt
```
</details>