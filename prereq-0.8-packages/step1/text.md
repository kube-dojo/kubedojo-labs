## Step 1: Update and Search Packages
1. Run `apt update` to refresh the package list.
2. Save the output of `apt show curl` to `~/curl-info.txt`.
<details>
<summary>Hint</summary>
```bash
sudo apt update
apt show curl > ~/curl-info.txt
```
</details>