## CoreDNS — Configuration, Service Discovery & DNS Debugging

Welcome to this hands-on lab from [KubeDojo](https://kube-dojo.github.io/) — free, open-source cloud native education.

You have a real Kubernetes cluster with one control plane node ready to use.

### Objectives

In this lab you will:

- Inspect and modify the CoreDNS ConfigMap (Corefile)
- Use fully qualified domain names (FQDNs) and short names for service discovery
- Debug DNS resolution failures using diagnostic tools

### Before You Start

- Read the [module page](https://kube-dojo.github.io/k8s/cka/part3-services-networking/module-3.3-dns/) for theory
- This lab takes approximately **35 minutes**
- `kubectl` is already configured and ready
- Alias `k=kubectl` is set up for speed

Let's begin!
