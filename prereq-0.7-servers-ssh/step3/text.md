## Step 3: Explore SSH Configuration
Find the SSH port number from `/etc/ssh/sshd_config` and save **just the number** to `~/ssh-port.txt`.
<details>
<summary>Hint</summary>
```bash
echo "22" > ~/ssh-port.txt
```
</details>