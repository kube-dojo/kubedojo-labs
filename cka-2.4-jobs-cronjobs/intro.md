## Jobs & CronJobs

Welcome to this hands-on lab from [KubeDojo](https://kube-dojo.github.io/) — free, open-source cloud native education.

You have a real Kubernetes cluster with one control plane node ready to use.

### Objectives

In this lab you will:

- Create a Job that runs a computation to completion
- Configure parallel Jobs with completions and parallelism
- Schedule recurring tasks with CronJobs

### Before You Start

- Read the [module page](https://kube-dojo.github.io/k8s/cka/part2-workloads-scheduling/module-2.4-jobs-cronjobs/) for theory
- This lab takes approximately **30 minutes**
- `kubectl` is already configured and ready
- Alias `k=kubectl` is set up for speed

Let's begin!
